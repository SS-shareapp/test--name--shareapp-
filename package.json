{
  "name": "shareapp-root",
  "private": true,
  "scripts": {
    "install:app": "cd src && bun install",
    "dev": "cd src && bun run dev",
    "build": "cd src && bun run build",
    "start": "cd src && bun run start",
    "lint": "cd src && bun run lint",
    "vercel:install": "npm install --save-dev vercel",
    "vercel:build": "vercel build"
  },
  "devDependencies": {
    "vercel": "^54.1.0"
  }
}
