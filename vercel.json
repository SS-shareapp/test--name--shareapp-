{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "bun --cwd src install --frozen-lockfile",
  "buildCommand": "bun --cwd src run build",
  "devCommand": "bun --cwd src run dev"
}
