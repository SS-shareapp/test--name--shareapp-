{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "if [ -d src ]; then cd src && npm ci; else npm ci; fi",
  "buildCommand": "if [ -d src ]; then cd src && npm run build; else npm run build; fi",
  "devCommand": "if [ -d src ]; then cd src && npm run dev; else npm run dev; fi"
}
